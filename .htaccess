<IfModule mod_rewrite.c>
    RewriteEngine On

    # Do not rewrite if already inside public/
    RewriteCond %{REQUEST_URI} !^/public/

    # Send everything through the public/ folder
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
